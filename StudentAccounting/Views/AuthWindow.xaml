<Window x:Class="StudentAccounting.Views.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="200" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" x:Name="LoginBox" Margin="0,5" Height="30" Text="dean"/>
        <PasswordBox Grid.Row="1" x:Name="PasswordBox" Margin="0,5" Height="30"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15">
            <Button Content="Вход" Width="100" Margin="5" Click="Login_Click"/>
            <Button Content="Выход" Width="100" Margin="5" Click="Exit_Click"/>
        </StackPanel>
        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="Red" TextAlignment="Center"/>
    </Grid>
</Window>